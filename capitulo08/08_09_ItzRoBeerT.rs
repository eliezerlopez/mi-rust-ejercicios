fn main() {
	let grados_celsius: f32 = 37.0;

	let grados_fahrenheit = grados_celsius * 9.0 / 5.0 + 32.0;

	println!("{} grados Celsius son {} grados Fahrenheit", grados_celsius, grados_fahrenheit);
}